

/*
when you make a reservation request,
and an admin approves the reservation request,
and you cancel the reservation request,
on the frontend, you'd also need to send an
email to the user (guest) of the listing
and the admin of the listing
*/
export async function POST(request) {}